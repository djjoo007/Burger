<li>
	{{burger_name}}

	<button class="eat" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour!{{else}}EAT!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
